# Examples

`demo_images/` contains six de-identified, resized 224 × 224 ultrasound images for demonstrating image format only. Labels, masks, clinical variables, and patient identifiers are not provided.

Use `scripts/create_toy_dataset.py` to generate a synthetic toy dataset for checking that the training and evaluation scripts run.
